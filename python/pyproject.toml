[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "signalux"
version = "0.1.0"
description = "SignaLux with Python API"
requires-python = ">=3.8"

[tool.hatch.build.targets.wheel]
packages = ["signalux"]
